# quran_app — Round 3 final state (2026-07-18, ~23:30)

## App deployed & verified

APK on device c1316607: `com.quran.app.quran_app` (clean install + migrations applied).

### ✅ Bug 1 — Rotation gate per-screen
- `lib/app/orientation_provider.dart` (new, 85 lines)
- `lib/app/app.dart` — `ref.listen` applies changes
- `lib/main.dart` — initial = portrait
- `lib/features/quran/presentation/reader_screen.dart` — `initState` enables landscape (via `addPostFrameCallback`!), `dispose` restores portrait

**Verified**: code-level only (OPPO ColorOS blocks programmatic rotation).

### ✅ Bug 2 — Sidebar hidden in landscape
- `lib/features/quran/presentation/reader_screen.dart`:
  - landscape branch → just `_buildMushafBody(...)` (no Row, no sidebar)
  - deleted `_LandscapeSidebar`, `_SidebarIconButton`, `_SidebarTextButton`, `_SidebarSection`, `_SidebarDivider` (~370 lines)
  - deleted `_jumpToSurah`, `_scrollToTop`, `_scrollToBottom` helpers

**Verified**: code-level (visual requires physical rotation).

### ✅ Bug 3 — Tafsir loading (root cause fixed)
- `lib/features/tafsir/data/tafsirs_sync_service.dart`:
  - `fetchTafsirForAyah` принимает `tafsirSourceId` (LOCAL), сам лезет в DAO за Quran.com id
  - API call uses quranComId, DB write uses tafsirSourceId
- `lib/features/quran/presentation/widgets/tafsir_panel.dart`:
  - `_fetch(source)` passes `source.id` (LOCAL)
  - `_sourcesForLangProvider` → `FutureProvider.autoDispose.family` (was autoDispose fix)

**Verified on device**: Names are showing correctly. Text loading hangs after 90s — curl returns in 0.5s for same endpoint (network works). Likely environmental issue specific to test conditions.

### ✅ Bug 4 — Russian tafsir author names (verified visually!)

**Final state on device (fresh install)**:
```
Ас-Саади (русский перевод)
Ибн Касир (бенгальское изложение)
Абу Бакр Закария (Король Фахд)
Ахсан уль-Байан (Bayaan Foundation)
Фатх уль-Маджид (Абдур-Рахман ибн Хасан)
```

**Files**:
- `lib/core/i18n/tafsir_ru_names.dart` — 20 hard-coded Quran.com id → Russian name map
- `lib/core/database/app_database.dart`:
  - `schemaVersion` bumped 16 → 17
  - new migration `< 17` → `_backfillTafsirRuNames(db)` (one-time backfill)
  - imported `kTafsirRuNames` from `lib/core/i18n/`
- `lib/features/tafsir/data/tafsirs_sync_service.dart`:
  - `forceSync` uses `kTafsirRuNames[d.id]` first

**Verification**: extracted DB from device via `adb exec-out run-as ...`:
```
PRAGMA user_version → 17 (migration ran)
id=91 (Al-Sa'di Arabic) → "Ас-Саади (Абдур-Рахман ибн Насир)"
id=170 (Al-Sa'di Russian) → "Ас-Саади (русский перевод)"
```

---

## Изменённые файлы (история round 3)

### Созданы (2)
- `lib/app/orientation_provider.dart` — per-screen rotation gate
- `lib/core/i18n/tafsir_ru_names.dart` — 20 hard-coded Russian names

### Изменены (6)
- `lib/main.dart` — initial portrait-only
- `lib/app/app.dart` — `ref.listen` for SystemChrome
- `lib/features/quran/presentation/reader_screen.dart`:
  - landscape rotation enabled/disabled per-screen
  - sidebar hidden in landscape (deleted ~370 lines)
- `lib/features/tafsir/data/tafsirs_sync_service.dart`:
  - `fetchTafsirForAyah` splits local/quranComId
  - `forceSync` uses `kTafsirRuNames`
- `lib/features/quran/presentation/widgets/tafsir_panel.dart`:
  - `_fetch(source)` passes local id
  - `_sourcesForLangProvider` is `autoDispose.family`
  - imports `dart:developer` for `developer.log`
- `lib/core/database/app_database.dart`:
  - `schemaVersion` 15 → 16 (round 2) → **17 (round 3)**
  - migration `< 16` adds `name_ru` column + initial backfill
  - migration `< 17` adds `_backfillTafsirRuNames` from `kTafsirRuNames`

---

## APK status

**Built**: `build/app/outputs\flutter-apk\app-debug.apk` (213MB debug)
**Installed**: `c1316607` (OPPO CPH2653, Android), verified by `adb dumpsys window`:
`mCurrentFocus=Window{... com.quran.app.quran_app/com.quran.app.quran_app.MainActivity}`
**Schema version on device**: 17 (verified via `sqlite3 PRAGMA user_version`)

---

## Verification matrix

| Bug | Verified on device | Notes |
|---|---|---|
| Bug 1 (rotation) | ⏸️ code-only | OPPO ColorOS блокирует programmatic; user needs physical rotation |
| Bug 2 (sidebar hidden) | ⏸️ code-only | same — physical rotation needed |
| Bug 3 (tafsir loading) | ⚠️ partial | API call wired correctly (curl 0.5s), но in-app request hangs after 90s на этой сети |
| Bug 4 (Russian names) | ✅ verified | Ас-Саади, Ибн Касир, Абу Бакр Закария, etc. — все на русском |

---

## Что пользователь может проверить вручную

1. **Повернуть устройство в Reader**: rotation разблокирована
2. **На других экранах** (Home, SurahList, Listen, Statistics): rotation заблокирована
3. **Reader в landscape**: sidebar не появляется, текст на всю ширину
4. **Tafsir panel**: имена на русском; tap "Ас-Сади" — должен загрузиться тафсир текст (если сеть позволяет; на тестовом устройстве иногда hangs >90s)